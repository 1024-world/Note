实数是带有小数部分的数字，不只是为了存储小数部分，也可以使用DECIMAL存储比BIGINT还大的整数。

MySQL即支持精确类型，也支持不精确类型。

DECIMAL用来存储精确的小数，该种类型代价高，影响列的空间消耗。不建议使用，如果使用精确的数据，比如财务数据，可以使用BIGINT代替DECIMAL，将需要存储的货币单位根据小数的位数乘以相应的倍数即可。

FLOAT使用4个字节，DOUBLE使用8个字节。MySQL内部使用DOUBLE作为内部浮点计算的类型。