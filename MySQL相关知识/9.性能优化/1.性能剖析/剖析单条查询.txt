1.使用show profile
可以在会话级别动态的开启该功能。
set profiling = 1;

该功能有一定的作用，但是在后期可能会被Perfoemance Schema所取代。

show profiles;
show profiling for query query_id;

使用show status
并不是一款剖析工具，其列出的大部分都是一个计数器，显示某些活动的频繁程度，但无法给出消耗了多少时间。

使用慢查询日志

使用Performance Schema

