MySQL中有两种类型的数字：整数和实数。

整数分为以下几种类型：TINYINT、SMALLINT、MEDIUMINT、INT、BIGINT，分别使用8、16、24、32、64位存储空间，存储的值的范围从负的2的N-1次方到2的N-1次方减一，N是存储空间的位数。

整数可选UNSIGNED属性，不允许负值，大致使正数的上限提高一倍。

MySQL可以为整数类型指定宽度，这不是限制值得合法范围，只是用来规定在交互工具中用来显示字符的个数。